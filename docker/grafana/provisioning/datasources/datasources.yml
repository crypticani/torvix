apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: cloudpulse
    user: cloudpulse
    secureJsonData:
      password: cloudpulse
    jsonData:
      sslmode: "disable"
      postgresVersion: 16
      timescaledb: true
