global:
  scrape_interval: 60s

scrape_configs:
  - job_name: torvix
    static_configs:
      # Keep this port aligned with Torvix's actual listener.
      # If you run with TORVIX_HTTP_PORT=18080 or http.address: ":18080",
      # change this target to "127.0.0.1:18080".
      - targets: ["127.0.0.1:8080"]
