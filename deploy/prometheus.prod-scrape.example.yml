scrape_configs:
  - job_name: torvix
    metrics_path: /metrics
    scrape_interval: 60s
    static_configs:
      - targets:
          # Change the port if Torvix is started with TORVIX_HTTP_PORT
          # or a non-default http.address.
          - torvix.example.internal:8080
